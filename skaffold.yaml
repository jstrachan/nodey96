apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/jstrachan/nodey96:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/jstrachan/nodey96:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: nodey96
        chartPath: charts/nodey96
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/jstrachan/nodey96'
          image.tag: '{{.DIGEST_HEX}}'
